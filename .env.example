# Deployment target (do NOT commit real values; copy to .env)
VOICE_HOST=<public IP or hostname of target server>
VOICE_SSH_USER=<SSH login user>
VOICE_SSH_KEY=<absolute path to SSH private key (.pem)>
